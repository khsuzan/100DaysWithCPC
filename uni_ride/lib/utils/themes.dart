import 'package:flutter/material.dart' show Color;

class MyColors {
  static const Color background = Color(0xFFFFFFFF); // White background
  static const Color backgroundLight = Color(0xFFF5F5F5); // Slightly off-white for light containers
  static const Color accent = Color(0xFF000000); // All accents are black
  static const Color accentDark = Color(0xFF000000); // Black for dark accent
  static const Color accentTransparent = Color(0x10000000); // Transparent black
  static const Color buttonShadow = Color(0x1A000000); // Subtle black shadow
  static const Color buttonForeground = Color(0xFF000000); // Black text/icons on buttons
  static const Color white = Color(0xFFFFFFFF);
  static const Color gray = Color(0xFFBDBDBD); // Neutral gray for borders, text
  static const Color grayLight = Color(0xFFF0F0F0); // Very light gray for backgrounds
  static const Color whiteTransparent = Color(0x25FFFFFF);
  static const Color black = Color(0xFF000000);
}

class MySize {}

class MyTypoGraphy {}
